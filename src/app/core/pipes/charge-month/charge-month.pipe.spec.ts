import { ChargeMonthPipe } from './charge-month.pipe';

describe('ChargeMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new ChargeMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
