import { ChargePipePipe } from './charge-pipe.pipe';

describe('ChargePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChargePipePipe();
    expect(pipe).toBeTruthy();
  });
});
