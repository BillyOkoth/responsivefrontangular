import { DetailReportPipe } from './detail-report.pipe';

describe('DetailReportPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailReportPipe();
    expect(pipe).toBeTruthy();
  });
});
