import { GroupSearchPipe } from './group-search.pipe';

describe('GroupSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
