import { BranchSearchPipe } from './branch-search.pipe';

describe('BranchSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BranchSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
