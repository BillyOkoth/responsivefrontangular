import { ReportSearchPipe } from './report-search.pipe';

describe('ReportSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
