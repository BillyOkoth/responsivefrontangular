import { HistoryReportsPipe } from './history-reports.pipe';

describe('HistoryReportsPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryReportsPipe();
    expect(pipe).toBeTruthy();
  });
});
