import { FailedAccountsPipe } from './failed-accounts.pipe';

describe('FailedAccountsPipe', () => {
  it('create an instance', () => {
    const pipe = new FailedAccountsPipe();
    expect(pipe).toBeTruthy();
  });
});
