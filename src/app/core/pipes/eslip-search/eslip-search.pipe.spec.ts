import { EslipSearchPipe } from './eslip-search.pipe';

describe('EslipSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EslipSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
