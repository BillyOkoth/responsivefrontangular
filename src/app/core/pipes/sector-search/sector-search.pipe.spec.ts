import { SectorSearchPipe } from './sector-search.pipe';

describe('SectorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SectorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
