import { RejectedSearchPipe } from './rejected-search.pipe';

describe('RejectedSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RejectedSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
