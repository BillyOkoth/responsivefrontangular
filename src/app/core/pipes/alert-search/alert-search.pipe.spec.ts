import { AlertSearchPipe } from './alert-search.pipe';

describe('AlertSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
