import { SearchPayerPipe } from './search-payer.pipe';

describe('SearchPayerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPayerPipe();
    expect(pipe).toBeTruthy();
  });
});
