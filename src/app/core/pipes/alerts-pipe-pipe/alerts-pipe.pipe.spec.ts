import { AlertsPipePipe } from './alerts-pipe.pipe';

describe('AlertsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlertsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
