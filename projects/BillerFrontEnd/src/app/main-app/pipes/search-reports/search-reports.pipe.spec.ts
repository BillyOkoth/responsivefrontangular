import { SearchReportsPipe } from './search-reports.pipe';

describe('SearchReportsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReportsPipe();
    expect(pipe).toBeTruthy();
  });
});
