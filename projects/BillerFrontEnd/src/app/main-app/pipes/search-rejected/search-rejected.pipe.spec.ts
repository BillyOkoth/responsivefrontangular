import { SearchRejectedPipe } from './search-rejected.pipe';

describe('SearchRejectedPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRejectedPipe();
    expect(pipe).toBeTruthy();
  });
});
