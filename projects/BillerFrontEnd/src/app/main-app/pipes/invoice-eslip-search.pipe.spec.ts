import { InvoiceEslipSearchPipe } from './invoice-eslip-search.pipe';

describe('InvoiceEslipSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceEslipSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
