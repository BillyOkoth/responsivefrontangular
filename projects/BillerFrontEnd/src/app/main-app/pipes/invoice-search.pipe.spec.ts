import { InvoiceSearchPipe } from './invoice-search.pipe';

describe('InvoiceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
