import { SearchFilePipe } from './search-file.pipe';

describe('SearchFilePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilePipe();
    expect(pipe).toBeTruthy();
  });
});
