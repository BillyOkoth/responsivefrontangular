import { PolicySearchPipe } from './policy-search.pipe';

describe('PolicySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PolicySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
