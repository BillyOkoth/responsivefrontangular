import { DetailSearchPipe } from './detail-search.pipe';

describe('DetailSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
