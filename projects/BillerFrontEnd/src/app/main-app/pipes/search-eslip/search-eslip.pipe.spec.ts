import { SearchEslipPipe } from './search-eslip.pipe';

describe('SearchEslipPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEslipPipe();
    expect(pipe).toBeTruthy();
  });
});
