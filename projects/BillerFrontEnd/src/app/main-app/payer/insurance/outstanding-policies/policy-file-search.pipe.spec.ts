import { PolicyFileSearchPipe } from './policy-file-search.pipe';

describe('PolicyFileSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PolicyFileSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
