import { CheckOffPolicySearchPipe } from './check-off-policy-search.pipe';

describe('CheckOffPolicySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckOffPolicySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
