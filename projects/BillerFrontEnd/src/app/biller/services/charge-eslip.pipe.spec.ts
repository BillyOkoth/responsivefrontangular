import { ChargeEslipPipe } from './charge-eslip.pipe';

describe('ChargeEslipPipe', () => {
  it('create an instance', () => {
    const pipe = new ChargeEslipPipe();
    expect(pipe).toBeTruthy();
  });
});
