import { PaidEslipsPipe } from './paid-eslips.pipe';

describe('PaidEslipsPipe', () => {
  it('create an instance', () => {
    const pipe = new PaidEslipsPipe();
    expect(pipe).toBeTruthy();
  });
});
