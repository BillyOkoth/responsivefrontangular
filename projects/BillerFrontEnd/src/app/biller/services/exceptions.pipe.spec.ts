import { ExceptionsPipe } from './exceptions.pipe';

describe('ExceptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExceptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
