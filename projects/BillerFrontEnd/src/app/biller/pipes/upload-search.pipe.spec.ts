import { UploadSearchPipe } from './upload-search.pipe';

describe('UploadSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UploadSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
