import { SearchReportPipe } from './search-report.pipe';

describe('SearchReportPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReportPipe();
    expect(pipe).toBeTruthy();
  });
});
