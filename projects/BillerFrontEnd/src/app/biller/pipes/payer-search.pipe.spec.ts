import { PayerSearchPipe } from './payer-search.pipe';

describe('PayerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PayerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
