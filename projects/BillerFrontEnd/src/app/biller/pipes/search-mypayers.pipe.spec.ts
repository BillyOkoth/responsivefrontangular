import { SearchMypayersPipe } from './search-mypayers.pipe';

describe('SearchMypayersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMypayersPipe();
    expect(pipe).toBeTruthy();
  });
});
