import { MypayersSearchPipe } from './mypayers-search.pipe';

describe('MypayersSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MypayersSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
