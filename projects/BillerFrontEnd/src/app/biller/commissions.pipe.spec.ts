import { CommissionsPipe } from './commissions.pipe';

describe('CommissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CommissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
