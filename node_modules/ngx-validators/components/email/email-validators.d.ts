import { AbstractControl, ValidatorFn } from '@angular/forms';
import { EmailOptions } from './email-util';
export declare class EmailValidators {
    private static emailSuggestion;
    static simple(control: AbstractControl): {
        [key: string]: boolean;
    };
    static normal(control: AbstractControl): {
        [key: string]: boolean;
    };
    static suggest(options?: EmailOptions): ValidatorFn;
}
